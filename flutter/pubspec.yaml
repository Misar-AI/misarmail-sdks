name: misarmail_flutter
description: Official Flutter SDK for MisarMail — secure key storage, retry, full API coverage for iOS, Android, macOS, Web.
version: 1.0.0
homepage: https://misarmail.com/docs/sdks/flutter
repository: https://github.com/Misar-AI/misarmail-sdks
issue_tracker: https://github.com/Misar-AI/misarmail-sdks/issues

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.10.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.0
  flutter_secure_storage: ^9.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  # No assets or fonts needed for SDK
