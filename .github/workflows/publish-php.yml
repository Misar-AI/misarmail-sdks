name: Publish PHP SDK (Packagist)
on:
  push:
    tags: ['php/v*']
  workflow_dispatch:
# Packagist mirrors from GitHub via webhook — this triggers the sync API.
#
# It must point at misarmail-php, not this monorepo: Packagist reads composer.json
# from the repository ROOT and cannot index a subdirectory. split-standalone.yml
# populates and tags that repo first, so run this after it.
jobs:
  notify-packagist:
    needs: []
    runs-on: ubuntu-latest
    steps:
      - name: Trigger Packagist update
        env:
          PACKAGIST_TOKEN: ${{ secrets.PACKAGIST_API_TOKEN }}
          PACKAGIST_USERNAME: ${{ secrets.PACKAGIST_USERNAME }}
        run: |
          curl -sSf -X POST \
            -H "Content-Type: application/json" \
            "https://packagist.org/api/update-package?username=${PACKAGIST_USERNAME}&apiToken=${PACKAGIST_TOKEN}" \
            -d '{"repository":{"url":"https://github.com/Misar-AI/misarmail-php"}}'
