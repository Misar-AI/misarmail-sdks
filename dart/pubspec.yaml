name: misarmail
description: Official Dart SDK for MisarMail — transactional email, campaigns, leads, CRM. Full API coverage with retry logic.
version: 1.0.0
homepage: https://misarmail.com/docs/sdks/dart
repository: https://github.com/Misar-AI/misarmail-sdks
issue_tracker: https://github.com/Misar-AI/misarmail-sdks/issues

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  http: ^1.2.0

dev_dependencies:
  test: ^1.25.0
  lints: ^3.0.0
  build_runner: ^2.4.9
