/*
 * AUTO-GENERATED by scripts/generate-sdks.mjs from sdks/manifest.json.
 * Do not edit by hand — changes are overwritten. To change this surface,
 * change the API route, re-run scripts/sync-sdk-manifest.mjs, then re-run
 * the generator.
 */
package io.misar.mail.generated

import io.misar.mail.core.Transport
import io.misar.mail.core.enc
import io.misar.mail.core.encodeQuery

class PreferencesResource(private val core: Transport) {
    /** GET /preferences — signed preference-centre token rather than an API key (no API key required) */
    suspend fun list(token: String, params: Map<String, String> = emptyMap()): Map<String, Any?> = core.request("GET", "/preferences" + encodeQuery(mapOf("token" to token) + params), null)

    /** PUT /preferences — signed preference-centre token rather than an API key (no API key required) */
    suspend fun update(body: Any? = null, params: Map<String, String> = emptyMap()): Map<String, Any?> = core.request("PUT", "/preferences" + encodeQuery(params), body)
}
